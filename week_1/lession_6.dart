
// 1. Tạo hàm `sayHi()` in “Xin chào!”.  
void sayhi ()
{
  print("Xin chào!");
}
// 2. Tạo hàm `square(x)` trả về `x * x`.  
// 3. Tạo hàm tính chu vi hình tròn (`r * 2 * 3.14`).  
// 4. Tạo hàm `greet(name, age)` in một câu giới thiệu.  
// 5. Tạo hàm `max(a, b)` trả về số lớn hơn.  
// 6. Tạo hàm kiểm tra số chẵn lẻ.  
// 7. Viết hàm nhận một List số và trả về tổng các phần tử.  
// 8. Tạo hàm `login({required username, required password})`.
void main ()
{
  sayhi();
}